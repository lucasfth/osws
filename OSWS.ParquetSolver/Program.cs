class Program
{
    static void Main()
    {
        // var asm = typeof(Parquet.ParquetReader).Assembly;
        // Console.WriteLine("Assembly: " + asm.FullName);
        // var types = asm.GetTypes().OrderBy(t => t.FullName).ToArray();
        // foreach (var t in types)
        // {
        //     Console.WriteLine(t.FullName);
        // }
    }
}
